"""MetricGuard — Semantic Conflict Intelligence for DataHub.

Deterministic core (parsing, signatures, comparison, divergence, drift),
LLM for judgment (clustering calls, explanations, canonical proposals).
The LLM never computes the verifiable math.
"""

__version__ = "0.1.0"
